[pytest]
pythonpath = .
testpaths = tests
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
markers =
    integration: requires live external services (Upstage API). Skipped by default.
addopts = -m "not integration"
filterwarnings =
    ignore::DeprecationWarning

[build]
dockerfilePath = "Dockerfile.api"

[deploy]
startCommand = "sh -c 'uv run uvicorn backend.main:app --host 0.0.0.0 --port ${PORT:-8001}'"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"

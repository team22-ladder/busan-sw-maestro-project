# app 패키지 컨텍스트

`app`은 백엔드 애플리케이션의 최상위 Python 패키지입니다.

현재 핵심 흐름은 FastAPI 라우터가 요청을 받아 `app/schedule_agent`의 LangGraph 일정 에이전트를 실행하고, 그래프 결과를 API 응답으로 반환하는 구조입니다.

이번 단계에서는 저장소, 프론트엔드, 인증은 다루지 않고 에이전트 실행 흐름만 다룹니다.

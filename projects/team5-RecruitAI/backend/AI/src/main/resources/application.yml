spring:
  # 가상 스레드 활성화 (Java 21 전용)
  threads:
    virtual:
      enabled: true

  # Redis 설정 (기본값)
  data:
    redis:
      host: localhost
      port: 6379

# 커스텀 설정 (AI 서버 통신용)
app:
  ai:
    server-url: http://localhost:8000/ai/analyze